//! Status bar component showing editor state.

/// Status bar showing cursor position, mode, encoding, etc.
pub struct StatusBar {
    // Status state
}

impl StatusBar {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for StatusBar {
    fn default() -> Self {
        Self::new()
    }
}
