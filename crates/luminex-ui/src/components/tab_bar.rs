//! Tab bar component for open documents.

/// Tab bar showing open documents.
pub struct TabBar {
    // Tab state
}

impl TabBar {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for TabBar {
    fn default() -> Self {
        Self::new()
    }
}
